package Java.Basic.Preparations;
/*

public class LinkedList {
    private ListNode head;
    static class ListNode{
        int data;
        ListNode next;

        ListNode(int data){
            this.data = data;
            next = null;
        }
    }

    public static void main(String[] args) {
        LinkedList ls = new LinkedList();
        ls.head = new ListNode(10);
        ListNode first = new ListNode(20);
        ListNode second = new ListNode(30);
        ListNode third = new ListNode(40);
        ls.head.next = first;
        first.next = second;
        second.next = third;

        addAtFirst(ls.head);

        while(ls.head!=null){
            System.out.print(ls.head.data+" ");
            ls.head=ls.head.next;
        }
    }

    private static void addAtFirst(ListNode head) {
        if(head==null) head;
    }
}
*/

